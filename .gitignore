# Build output
build/

# CMake / tool caches
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Makefile
compile_commands.json

# Qt / QML tooling
*.qmlc
*.jsc
*.rcc
.qmake.stash

# IDE/editor
.vscode/
.idea/
*.user
*.user.*

# OS
Thumbs.db
.DS_Store

# Credentials -- never commit these
raspberrypi_login_info.txt
*login_info*
*credentials*
*.pem
*.key

# Vendor documentation -- deliberately untracked.
# Third-party manuals we did not author, and large binaries git would keep
# forever. The facts we depend on are transcribed into docs/ with page
# citations; see docs/LithiumBalance_BMS_CAN_Reference.md for the exact
# version to obtain and where to put it.
docs/vendor/
